<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/editText"
        android:layout_width="200dp"
        android:layout_height="35dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="42dp"
        android:background="@layout/my_edit_text_border"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="@string/url_for_edit_Text"
        tools:ignore="LabelFor" />

    <Button
        style="@style/buttonsStyle"
        android:id="@+id/take_picture_button"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/editText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:text="@string/takePictureButton" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignEnd="@+id/take_picture_round_button"
        android:layout_below="@+id/textViewForBuild"
        android:layout_marginTop="28dp" />

    <TextView
        android:id="@+id/textViewForBuild"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/take_picture_round_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp" />

    <Button
        style="@style/buttonsStyle"
        android:id="@+id/take_picture_round_button"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/take_picture_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="14dp"
        android:text="@string/buttonRound" />

</RelativeLayout>