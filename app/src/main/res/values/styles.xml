<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/defaultColo</item>
        <item name="colorPrimaryDark">@color/defaultColo</item>
        <item name="colorAccent">@color/defaultColo</item>
    </style>
</resources>
